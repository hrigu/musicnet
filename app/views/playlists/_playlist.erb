<tr>

  <td>
    <%= link_to playlist_path(playlist.id) do %>
      <span class="badge <%= playlist.color_class %>"><%= playlist.short_name %></span>
    <% end %>
  </td>
  <td><%= playlist.public ? "öffentlich" : "privat" %></td>
  <td><%= playlist.tracks.count  %></td>
  <td><%= link_to(playlist.name, download_playlist_path(playlist.id))  %></td>
  <td><%= link_to("edit", edit_playlist_path(playlist))  %></td>
</tr>
