# 11 - Test (Rest): Playlist-Model, User-Model und Controller testen

## Why

Letzte ungetestete Bereiche der App — Playlist-Model-Helpers, User-Model (Login-Erstellung) und
sämtliche Controller (aktuell 0% Controller-Coverage).

## What / How

TDD, ein Szenario nach dem anderen, jeweils zielgerichtet `bundle exec rspec` auf die betroffene Datei.

* [x] 1.0 - Playlist-Model testen:
    - [x] 1.1 - `#short_name` ersetzt "fusion"/"blues" und entfernt Leerzeichen
    - [x] 1.2 - `#color_class` gibt 'bg-dark' für DJ-Playlists (Name beginnt mit 4 Ziffern), sonst einen Wert aus CONTEXT
    - [x] 1.3 - `#name_path_ready` entfernt Leerzeichen aus dem Namen
* [x] 2.0 - User-Model testen:
    - [x] 2.1 - `.from_omniauth` erstellt einen neuen User, wenn noch keiner existiert
    - [x] 2.2 - `.from_omniauth` findet den bestehenden User anhand provider+uid (kein Duplikat)
    - [x] 2.3 - `#spotify_user` rekonstruiert ein RSpotify::User aus `spotify_user_data`
* [x] 3.0 - Test-Infrastruktur für Request-Specs aktivieren (Devise Sign-in-Helper für `type: :request`)
* [x] 4.0 - PlaylistsController testen (Request-Specs):
    - [x] 4.1 - Ohne Login: Redirect zum Sign-in
    - [x] 4.2 - `index`/`show` liefern Erfolg
    - [x] 4.3 - `fetch_all` ruft BuildMusicNetService auf (gestubbt) und liefert Erfolg
    - [x] 4.4 - `download` ruft DownloadPlaylistService auf (gestubbt) und redirected zu `playlist_path`
* [x] 5.0 - TracksController testen:
    - [x] 5.1 - `index`/`show`/`recently_played_index` liefern Erfolg (Spotify-API gestubbt)
    - [x] 5.2 - `download` ruft DownloadTrackService auf (gestubbt) und redirected zu `tracks_path`
    - [x] 5.3 - `stream` sendet die Datei, wenn `track_path` vorhanden ist
* [x] 6.0 - ArtistsController testen:
    - [x] 6.1 - `index`/`show` liefern Erfolg
* [x] 7.0 - UsersController testen:
    - [x] 7.1 - `#spotify`-Callback loggt den User ein und redirected (OmniAuth gemockt)
