# 41 - Feature (Tracks-Player): Song-Queue

## 1. Why

### 1.1 Objective

Tracks sollen sich in eine Warteschlange einreihen lassen, statt immer nur einen einzelnen Track
sofort zu ersetzen (Intent 40). Ist der aktuelle Track fertig, startet automatisch der nächste aus
der Queue — der DJ kann sich damit mehrere Songs im Voraus zurechtlegen, ohne nach jedem Track neu
klicken zu müssen.

### 1.2 Context

Baut direkt auf Intent 40 auf: der globale, dauerhafte Mini-Player (`layouts/_audio_player.html.erb`,
`data-turbo-permanent`) übersteht bereits Turbo-Navigation (Frame-Updates und volle Seitenwechsel).
Die Queue ist reiner Client-State (ein JS-Array im `audio-player`-Controller) — kein neuer
DB-Zustand, keine neue Route nötig. Da Turbo permanente Elemente unangetastet lässt (kein
Neu-Verbinden des Controllers), überlebt dieser In-Memory-State genau wie die laufende Wiedergabe
selbst automatisch jede Navigation. Wie bei Intent 40 kein Persistieren über einen echten
Seiten-Reload (F5) hinweg — nur über Turbo-Navigation.

Wieder reines Frontend/JS, keine RSpec-Abdeckung für die eigentliche Queue-Logik möglich ausser
über die in Intent 40 neu eingeführten System-Specs (`capybara` + `cuprite`).

### 1.3 Related to Intents

Baut auf [Intent 40](../completed/40.feature_tracks-player_dauerhafte-wiedergabe.md) auf
(dauerhafter globaler Player, Event-basierte Entkopplung zwischen Trigger-Buttons und Player).

## 2.0 What

```gherkin
Feature: Song-Queue

  Als DJ
  moechte ich Tracks in eine Warteschlange einreihen,
  damit nach dem aktuellen Track automatisch die vorgemerkten Songs weiterspielen.

  Scenario: Track zur Queue hinzufuegen
    Given ein Track hat ein Soundfile
    When ich auf dessen "zur Queue hinzufuegen"-Button klicke
    Then erscheint der Track in der Queue-Anzeige oberhalb des Players

  Scenario: Automatischer Wechsel zum naechsten Track
    Given der aktuelle Track spielt und die Queue enthaelt mindestens einen Track
    When der aktuelle Track zu Ende ist
    Then startet automatisch der erste Track aus der Queue
    And er wird aus der Queue-Anzeige entfernt

  Scenario: Maximale Queue-Groesse
    Given die Queue enthaelt bereits 5 Tracks
    When ich einen weiteren Track hinzufuegen will
    Then bleibt die Queue unveraendert bei 5 Tracks (Klick hat keine Wirkung)

  Scenario: Track manuell aus der Queue entfernen
    Given ein Track befindet sich in der Queue
    When ich auf dessen Entfernen-Button in der Queue-Anzeige klicke
    Then verschwindet er aus der Queue, ohne die Wiedergabe zu beeinflussen

  Scenario: Queue ueberlebt Navigation
    Given die Queue enthaelt Tracks
    When ich suche/sortiere/paginiere oder auf eine andere Seite wechsle
    Then bleibt die Queue unveraendert sichtbar
```

## 3.0 How

### 3.1 Implementation Context

* **Queue-State im bestehenden `audio_player_controller.js`** (kein neuer Controller): ein
  einfaches In-Memory-Array (`{ url, name }`-Objekte), max. Länge 5
  (`MAX_QUEUE_SIZE`-Konstante). Rendering der Queue-Liste direkt per JS (`innerHTML`/Template) in
  einen neuen `queueTarget`-Container — kein Rails-Partial-Rendering nötig, da rein clientseitig.
* **Neuer Button pro Track** in `components/_audio_file.html.erb`, neben dem bestehenden
  Play-Button: "zur Queue hinzufügen" (z. B. "+"-Icon). Nutzt denselben
  `audio_trigger_controller.js` (kennt bereits `url`/`name` der Zeile) mit einer zweiten Action
  `enqueue()`, die ein neues Event `audio-player:enqueue` dispatcht (statt `audio-player:play`)
  — gleiches Entkopplungsmuster wie in Intent 40.
* **Queue-Anzeige**: neuer Bereich in `layouts/_audio_player.html.erb`, **oberhalb** der
  bestehenden Play/Pause-Zeile (damit die Queue sichtbar "ueber" dem aktuell laufenden Track
  steht, wie gefordert). Pro Queue-Eintrag: Trackname + kleiner ×-Button
  (`data-action="audio-player#removeFromQueue"` mit dem Index als Parameter). Leere Queue zeigt
  einen Platzhalter ("Queue leer"), analog zum "–"-Platzhalter für "kein Track spielt" (Intent 40).
* **Verhalten bei voller Queue**: `enqueue()` prüft `this.queue.length >= 5` und ignoriert den
  Klick stillschweigend (kein Hinweis/Fehler) — siehe Klärung oben.
* **Auto-Advance**: der bestehende `ended`-Event-Handler auf dem `<audio>`-Element (aktuell nur
  fürs Icon-Reset zuständig) wird erweitert: ist die Queue nicht leer, wird deren erster Eintrag
  per `dequeue()` entnommen und abgespielt (gleicher Pfad wie `handlePlayEvent`); ist die Queue
  leer, bleibt das bisherige Verhalten (Icon zurück auf "▶").
* **Bewusst ausserhalb des Scopes:** kein Umsortieren der Queue per Drag & Drop; kein
  "als nächstes abspielen" (Einfügen vorne statt hinten); kein Persistieren über einen echten
  Seiten-Reload.

### 3.2 Tasks

* [x] 1.0 - Queue-Datenstruktur und Anzeige
    - [x] 1.1 - `layouts/_audio_player.html.erb`: neuer Queue-Bereich (`#audio-player-queue`)
          oberhalb der Play/Pause-Zeile, Platzhalter "Queue leer"
    - [x] 1.2 - `audio_player_controller.js`: `queueEntries`-Array (max. 5, `MAX_QUEUE_SIZE`),
          `renderQueue()` baut die Liste (Trackname + ×-Button pro Eintrag) im `queueTarget` neu auf
* [x] 2.0 - Track zur Queue hinzufügen
    - [x] 2.1 - `audio_trigger_controller.js`: neue Action `enqueue()`, dispatcht
          `audio-player:enqueue` mit `{ url, name }`
    - [x] 2.2 - `components/_audio_file.html.erb`: zweiter Button ("+", title "Zur Queue
          hinzufügen") pro Track-Zeile, beide Buttons teilen sich eine `audio-trigger`-Instanz
    - [x] 2.3 - `audio_player_controller.js`: lauscht auf `audio-player:enqueue`, hängt an (falls
          `queueEntries.length < 5`, sonst stiller No-op), rendert neu
* [x] 3.0 - Entfernen und Auto-Advance
    - [x] 3.1 - `audio_player_controller.js`: `removeFromQueue(event)` (Index als Stimulus-
          Action-Param), entfernt Eintrag, rendert neu
    - [x] 3.2 - `audio_player_controller.js`: `ended`-Handler → `playNextInQueue()`, spielt bei
          nicht-leerer Queue automatisch deren ersten Eintrag, sonst Icon zurück auf "▶"
* [x] 4.0 - Verifikation (System-Specs, TDD rot/grün wie in Intent 40)
    - [x] 4.1 - Track zur Queue hinzufügen → erscheint in der Anzeige
    - [x] 4.2 - Queue bei 5 Einträgen → sechster Klick ändert nichts (Assertion auf genau 5
          `.queue-entry`-Elemente)
    - [x] 4.3 - Manuelles Entfernen eines Eintrags
    - [x] 4.4 - Automatischer Wechsel zum nächsten Track nach Ende des aktuellen (`ended`-Event
          per `execute_script` ausgelöst, ohne echte abspielbare Datei)
    - [x] 4.5 - Queue übersteht Suche (Turbo-Frame-Update) und Seitenwechsel
          (`spec/system/song_queue_spec.rb`, gleiches Muster wie Intent 40s bestehende Specs);
          gemeinsame Helfer (`create_playable_track`, `play_button_for`, `enqueue_button_for`)
          nach `spec/support/playback_test_helpers.rb` extrahiert, `persistent_audio_player_spec.rb`
          darauf umgestellt
* [x] 5.0 - Abschluss
    - [x] 5.1 - Volle Suite (211 Examples) + Rubocop-Delta geprüft (nur bestehende
          Offense-Muster im Delta), visueller Screenshot-Check (Queue-Liste oberhalb der
          Player-Leiste, kein Überlappen)
    - [x] 5.2 - CLAUDE.md ergänzt (Queue-Architektur, Verweis auf Intent 40)

### 3.3 Nachtrag: Anzeigereihenfolge umgedreht

Erstes Feedback nach dem Ausprobieren: die Reihenfolge war zwar korrekt FIFO (`push`/`shift`),
aber kontraintuitiv angezeigt (als nächstes gespielter Track zuoberst). Korrigiert: `renderQueue()`
zeigt die Liste jetzt umgekehrt zur internen Array-Reihenfolge - der als nächstes gespielte Track
(`queueEntries[0]`) steht zuunterst (direkt über der Player-Leiste), neu hinzugefügte erscheinen
zuoberst. Der interne Zustand (Reihenfolge in `queueEntries`, `removeFromQueue`-Index) ist
unverändert, nur die Darstellung wurde gespiegelt. Zwei neue Specs in `song_queue_spec.rb` sichern
das ab (Anzeigereihenfolge explizit geprüft; Entfernen des richtigen Eintrags bei mehreren
Warteschlangen-Einträgen).

### 3.4 Nachtrag: Player-Play-Button startet Queue bei frischem Player

Zweites Feedback: war noch nie ein Track geladen (`audioTarget.src` leer) oder der aktuelle Track
bereits zu Ende (`ended`), tat der Player-eigene Play-Button nichts, selbst wenn die Queue gefüllt
war - `audioTarget.play()` auf einem leeren/beendeten Element hat keine sinnvolle Wirkung.
`toggle()` prüft das jetzt vorab und ruft in diesem Fall `playNextInQueue()` auf, statt
`play()`/`pause()` auf das `<audio>`-Element anzuwenden. Neuer Button-Titel "Play/Pause" für
stabile Testauswahl (`player_toggle_button`-Helfer). Neue Spec in `song_queue_spec.rb` sichert das
ab.

### 3.5 Nachtrag: Queue verschwand bei Navigation über einen `_top`-Frame-Escape-Link

Drittes, gravierenderes Feedback: Navigation von `/tracks` zu z. B. `/artists/:id` über einen
Artist-/Tracknamen-Link (`data: { turbo_frame: "_top" }`, dient dem Ausbrechen aus
`turbo_frame_tag "tracks"`, siehe Architektur-Doku zu `tracks/_track.erb`) leerte die Queue bis auf
den zuletzt hinzugefügten Track. Ursache: anders als ein gewöhnlicher Top-Level-Link (z. B. der
Navbar-Link "Playlists", mit dem Intent 40/41 ursprünglich getestet wurde) löst so ein
Frame-Escape-Link bei Turbo einen internen Ablauf aus, der den `audio-player`-Stimulus-Controller
auf dem permanenten Element neu verbindet (`connect()` läuft erneut) - der DOM-Knoten selbst bleibt
zwar erhalten (deshalb blieb die Anzeige bis zum nächsten Rendern optisch unverändert), aber jede
Instanzvariable des Controllers (`this.queueEntries`) ging dabei verloren. Behoben, indem die Queue
nicht mehr als Instanzvariable, sondern als Property direkt auf `this.element` (`audioPlayerQueueEntries`)
gehalten wird - dieses Element ist nachweislich immer derselbe DOM-Knoten, unabhängig vom
Stimulus-Lifecycle. Aus demselben Grund wurden alle Event-Listener auf dem `<audio>`-Element von
anonymen auf benannte, gebundene Funktionen umgestellt, damit `disconnect()` sie bei einem
künftigen Reconnect sauber entfernen kann statt sie zu duplizieren (sonst hätte z. B. `ended`
irgendwann mehrfach pro tatsächlichem Trackende gefeuert). Neue Spec in `song_queue_spec.rb`
reproduziert exakt den gemeldeten Navigationspfad (Artist-Link mit `_top`) und schlug vor dem Fix
fehl.

### 3.6 Nachtrag: Künstler + Playlists zusätzlich zum Titel in der Queue-Anzeige

Viertes Feedback: die Queue-Einträge sollten neben dem Titel auch Künstler und Playlist(s) zeigen.
Neue Helper `TracksHelper#artist_names_for`/`#playlist_names_for` (mit Test in
`spec/helpers/tracks_helper_spec.rb`) - Letzterer nutzt `track.playlist_tracks`, falls das bereits
preloaded ist (z. B. `/tracks`), sonst `track.playlists` (z. B. `tracks#show`), da je nach
Aufrufkontext nur eine der beiden Assoziationen preloaded ist und die andere mit `strict_loading`
einen Fehler auslösen würde. `components/_audio_file.html.erb` gibt Künstler/Playlist-Namen als
zwei zusätzliche Stimulus-Values an `audio_trigger_controller.js` weiter, die sie im Event-Detail
mitschickt; `renderQueue()` zeigt sie als zweite, kleinere Zeile unter dem Titel. Dabei auch eine
vorbestehende, nie ausgenutzte aber echte XSS-Lücke behoben: `renderQueue()` fügte `entry.name`
bisher ungeschützt per `innerHTML` ein - jetzt escaped eine neue `escapeHtml()`-Methode alle in die
Queue eingefügten Werte (Titel, Künstler, Playlists).
